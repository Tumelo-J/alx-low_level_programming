#include <stdio.h>

/**
 * main - Program Entry
 * Description: Printing combinations of three digit numbers
 * Return: 0
 */
int main(void)
{
	int i;
	int j;
	char ic = itoa(i);
	char jc = itoa(j);
	for (i ='0';i <= '9'; i++;
			for (j = '0'; j<='9'; j++)
				if (i != j && "icjc" != "jcic")
					putchar(ij)
	putchar('\n');
	return (0);
}
